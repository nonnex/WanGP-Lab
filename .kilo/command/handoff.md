---
description: End-of-session handoff notes for WanGP-Lab
---

Write/update a short handoff under `suite/docs/` or `data/experiments/` covering:

1. What ran (Move/FastWan, seeds, steps, profile)
2. Gate results (open_end ok/progress/dy/dx)
3. Blockers (OOM, tracks, hand glue)
4. Next command (exact bash)

Also remind:

```bash
cd ~/AI/Projects/WanGP-Lab
git status
# push suite only — wangp/ is local
```
